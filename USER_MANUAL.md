# 🛠️ คู่มือการติดตั้งและสร้างระบบ (Complete Setup Guide)
**โครงการ:** ระบบบ่อปลาอัจฉริยะ (Smart Fish Pond System)

คู่มือฉบับนี้จะอธิบายขั้นตอนการสร้างระบบตั้งแต่การเตรียมอุปกรณ์ การตั้งค่าเซิร์ฟเวอร์ การอัปโหลดโค้ด ไปจนถึงการใช้งานจริง สำหรับผู้ที่ต้องการนำโปรเจกต์นี้ไปพัฒนาต่อ

---

## 🔌 ส่วนที่ 1: การเตรียมอุปกรณ์ฮาร์ดแวร์และการต่อวงจร

**รายการอุปกรณ์ที่ใช้:**
1. บอร์ดไมโครคอนโทรลเลอร์ **ESP32**
2. เซนเซอร์วัดอุณหภูมิน้ำ **DS18B20**
3. โมดูลเซนเซอร์วัดแสงสว่าง **LDR (Photoresistor)**
4. **โมดูลรีเลย์ 2 ช่อง** (2-Channel Relay)
5. หลอดไฟ LED 3 สี (เขียว, เหลือง, ฟ้า)

**การต่อสายสัญญาณ (Pin Mapping):**
| อุปกรณ์ | ขาของอุปกรณ์ | ขาบน ESP32 |
| :--- | :--- | :--- |
| **DS18B20** | DATA | `GPIO 4` |
| **LDR Sensor** | Analog Out | `GPIO 34` |
| **Relay CH 1** (พัดลม) | IN1 | `GPIO 18` |
| **Relay CH 2** (ไฟ) | IN2 | `GPIO 19` |
| **LED สีเขียว** (WiFi) | Anode (+) | `GPIO 13` |
| **LED สีเหลือง** (แจ้งเตือน) | Anode (+) | `GPIO 15` |
| **LED สีฟ้า** (ส่งข้อมูล) | Anode (+) | `GPIO 5` |
| **ปุ่ม BOOT** (รีเซ็ต WiFi) | - | `GPIO 0` |

---

## 💻 ส่วนที่ 2: การตั้งค่าฝั่งเซิร์ฟเวอร์และฐานข้อมูล (XAMPP Server)

1. **ติดตั้งและเปิด XAMPP:** เปิดโปรแกรม XAMPP Control Panel แล้วกด **Start** ที่โมดูล `Apache` และ `MySQL`
2. **สร้างฐานข้อมูล:**
   * เปิดเว็บบราวเซอร์ พิมพ์ `localhost/phpmyadmin`
   * สร้างฐานข้อมูลใหม่ชื่อ `smart_pond_project`
3. **นำเข้าฐานข้อมูล (Import):**
   * นำไฟล์ `.sql` ในโปรเจกต์นี้ไป Import เข้าฐานข้อมูล หรือรันคำสั่ง SQL สร้างตาราง `sensor_log` และ `control_status`
4. **ติดตั้งไฟล์เว็บไซต์:**
   * สร้างFolderและนำไฟล์ PHP ทั้งหมดไปวางไว้ในโฟลเดอร์ `C:\xampp\htdocs\smart_pond_project\`
5. **หาหมายเลข IP ของเซิร์ฟเวอร์:**
   * เปิด Command Prompt (cmd) พิมพ์คำสั่ง `ipconfig` จดค่า **IPv4 Address** ไว้ใช้ในขั้นตอนต่อไป

---

## 🤖 ส่วนที่ 3: การตั้งค่า Telegram Bot

1. เปิดแอป Telegram ค้นหาบอทชื่อ **`@BotFather`** พิมพ์คำสั่ง `/newbot` เพื่อสร้างบอทและรับ **Bot Token**
2. ค้นหาบอทชื่อ **`@userinfobot`** เพื่อหาหมายเลข **Chat ID** ของคุณ
3. นำ `Bot Token` และ `Chat ID` ไปใส่เตรียมไว้ในซอร์สโค้ด

---

## 📥 ส่วนที่ 4: การอัปโหลดโค้ดลง ESP32

1. **ติดตั้งไลบรารีใน Arduino IDE:** * `WiFiManager`
   * `DallasTemperature`
   * `OneWire`
2. **ตั้งค่าในซอร์สโค้ด:**
   * เปิดไฟล์ `sketch.ino`
   * แก้ไข IP Address ในตัวแปร `serverName` และ `controlName` ให้ตรงกับ IPv4 ของเครื่องเซิร์ฟเวอร์
   * ตรวจสอบรหัส API Key ให้ตรงกับไฟล์ `insert_data.php`
3. **อัปโหลด:** เลือก Port ให้ถูกต้อง แล้วกด Upload

---

## 🚀 ส่วนที่ 5: การเริ่มใช้งานจริง

1. **ตั้งค่าเครือข่ายครั้งแรก (Captive Portal):**
   * จ่ายไฟให้บอร์ด ระบบจะปล่อย WiFi ชื่อ **`SmartPond_Setup`**
   * เชื่อมต่อ WiFi นี้ด้วยรหัสผ่าน `12345678` 
   * เข้าหน้าตั้งค่า (Configure WiFi) เพื่อเลือกเครือข่ายอินเทอร์เน็ตที่ต้องการ
2. **การใช้งาน Dashboard:**
   * เข้าใช้งานหน้าเว็บผ่าน `http://[IP-Address]/smart_pond_project/index.php`
   * เลือกสั่งงานได้ทั้งโหมด **AUTO** และ **MANUAL**
3. **การสังเกตการทำงาน:**
   * ทุกๆ 10 วินาที ไฟ LED สีฟ้าจะกระพริบ 1 ครั้ง (แปลว่าส่งข้อมูลเข้า Database สำเร็จ)
   * ถ้าน้ำร้อนเกิน 32°C ไฟ LED สีเหลืองจะติด พัดลมจะหมุน และ Telegram จะเด้งเตือนทันที!     
4. **การล้างข้อมูล (Factory Reset):**
   * กดปุ่ม BOOT ค้างไว้ **3 วินาที** ระบบจะล้างรหัส WiFi ทิ้งและรอรับการตั้งค่าใหม่
   * ระบบจะลบรหัสผ่านเดิมทิ้ง แจ้งเตือนเข้า Telegram แล้วรีสตาร์ทกลับไปปล่อย WiFi ตัวเองอีกครั้ง
  
     ---

## ส่วนที่ 6: ความหมายของไฟแสดงสถานะ (LED Indicators)
ระบบมีไฟ LED เพื่อสื่อสารสถานะการทำงานต่างๆ ให้ผู้ใช้งานทราบได้อย่างรวดเร็ว:

* 🟢 **LED สีเขียว (สถานะ WiFi):**
  * **ดับ:** ไม่มีสัญญาณอินเทอร์เน็ต หรือกำลังเริ่มต้นระบบ
  * **กระพริบเป็นจังหวะ:** อินเทอร์เน็ตหลุด ระบบกำลังพยายามเชื่อมต่อใหม่
  * **ติดค้าง:** เชื่อมต่อ WiFi สำเร็จ และระบบออนไลน์เป็นปกติ
* 🟡 **LED สีเหลือง (สถานะการแจ้งเตือน):**
  * **ติดค้าง:** ระบบตรวจพบความผิดปกติ (เช่น อุณหภูมิน้ำสูงกว่า 32°C) และพัดลมกำลังทำงาน
  * **ดับ:** สภาพแวดล้อมอยู่ในเกณฑ์ปกติ
* 🔵 **LED สีฟ้า (สถานะการส่งข้อมูล):**
  * **กระพริบ 1 ครั้ง (ทุกๆ 10 วินาที):** ระบบทำการส่งข้อมูลเซนเซอร์ขึ้นไปเก็บบนฐานข้อมูล (Database) สำเร็จ

---

## ส่วนที่ 7: การใช้งาน Web Dashboard และแผงควบคุม
ผู้ใช้สามารถตรวจสอบค่าเซนเซอร์ต่างๆ และสั่งการระบบได้ผ่านหน้าเว็บ โดยแผงควบคุมรองรับการทำงาน 2 โหมด:

### 🤖 โหมดระบบอัตโนมัติ (AUTO)
เป็นโหมดการทำงานมาตรฐาน ระบบจะล็อคปุ่มกดบนหน้าเว็บ และใช้เซนเซอร์ควบคุมฮาร์ดแวร์โดยตรง
* **พัดลมระบายความร้อน:** จะเปิดอัตโนมัติเมื่อน้ำมีอุณหภูมิสูงกว่า 32°C และปิดเมื่ออุณหภูมิลดลงต่ำกว่า 30°C
* **ไฟส่องสว่าง:** จะเปิดอัตโนมัติเมื่อเซนเซอร์ LDR ตรวจพบความมืดต่อเนื่อง 3 วินาที และปิดอัตโนมัติเมื่อมีแสงสว่างเพียงพอ

### 🖐️ โหมดควบคุมเอง (MANUAL)
* ผู้ใช้สามารถกดปุ่มสลับโหมดเป็น MANUAL บนหน้าเว็บ เพื่อปลดล็อคการควบคุมแบบอัตโนมัติ
* ผู้ใช้สามารถกดปุ่ม **"เปิด (ON)"** หรือ **"ปิด (OFF)"** เพื่อสั่งการพัดลมระบายความร้อนและไฟส่องสว่างได้ตามต้องการผ่านหน้าเว็บทันที

---

## ส่วนที่ 8: รูปแบบการแจ้งเตือน (Telegram Notifications)
ระบบจะส่งข้อความแจ้งเตือนไปยังผู้ดูแลระบบผ่านแอปพลิเคชัน Telegram ทันทีเมื่อเกิดเหตุการณ์ดังต่อไปนี้:
* 🟢 **ระบบพร้อมใช้งาน:** เมื่อเปิดเครื่องและเชื่อมต่ออินเทอร์เน็ตสำเร็จ
* ⚠️ **แจ้งเตือนความร้อน:** เมื่ออุณหภูมิน้ำสูงเกินกำหนด (พร้อมแจ้งสถานะการเปิดพัดลม)
* ✅ **อัปเดตสถานะปกติ:** เมื่ออุณหภูมิน้ำลดลงกลับสู่สภาวะปกติ (พร้อมแจ้งสถานะการปิดพัดลม)
* 💡 **แจ้งเตือนแสงสว่าง:** เมื่อบรรยากาศมืดลงและระบบทำการเปิดไฟส่องสว่าง หรือเมื่อสว่างเพียงพอและระบบปิดไฟ
* 🔄 **แจ้งเตือนการรีเซ็ต:** เมื่อมีการสั่งล้างรหัสผ่าน WiFi จากตัวเครื่อง

---
