📘 คู่มือการติดตั้งและใช้งานระบบตั้งแต่เริ่มต้น (Complete Setup Guide)
โครงการ: ระบบบ่อปลาอัจฉริยะ (Smart Fish Pond System)

คู่มือฉบับนี้จะอธิบายขั้นตอนการสร้างระบบตั้งแต่การเตรียมอุปกรณ์ การตั้งค่าเซิร์ฟเวอร์ การอัปโหลดโค้ด ไปจนถึงการใช้งานจริง

🛠️ ส่วนที่ 1: การเตรียมอุปกรณ์ฮาร์ดแวร์และการต่อวงจร
รายการอุปกรณ์ที่ใช้:

บอร์ดไมโครคอนโทรลเลอร์ ESP32

เซนเซอร์วัดอุณหภูมิน้ำ (DS18B20)

โมดูลเซนเซอร์วัดแสงสว่าง (LDR Photoresistor)

โมดูลรีเลย์ 2 ช่อง (2-Channel Relay) สำหรับควบคุมพัดลมและไฟส่องสว่าง

หลอดไฟ LED 3 สี (เขียว, เหลือง, ฟ้า)

สวิตช์ปุ่มกด (Push Button) สำหรับรีเซ็ต WiFi

การต่อสายสัญญาณ (Pin Mapping):

GPIO 4 ต่อกับ เซนเซอร์อุณหภูมิ DS18B20 (ขา DATA)

GPIO 34 ต่อกับ เซนเซอร์แสง LDR (ขา Analog Out)

GPIO 18 ต่อกับ รีเลย์ช่อง 1 (ควบคุมพัดลมระบายความร้อน)

GPIO 19 ต่อกับ รีเลย์ช่อง 2 (ควบคุมไฟส่องสว่าง)

GPIO 13 ต่อกับ LED สีเขียว (แสดงสถานะ WiFi)

GPIO 15 ต่อกับ LED สีเหลือง (แสดงสถานะแจ้งเตือน)

GPIO 5 ต่อกับ LED สีฟ้า (แสดงสถานะการส่งข้อมูล)

GPIO 0 คือปุ่ม BOOT บนบอร์ด (ใช้เป็นปุ่มรีเซ็ต WiFi)

💻 ส่วนที่ 2: การตั้งค่าฝั่งเซิร์ฟเวอร์และฐานข้อมูล (XAMPP Server)
ระบบนี้ใช้คอมพิวเตอร์จำลองเป็นเซิร์ฟเวอร์ผ่านโปรแกรม XAMPP

ติดตั้งและเปิด XAMPP: เปิดโปรแกรม XAMPP Control Panel แล้วกดปุ่ม Start ที่โมดูล Apache และ MySQL

สร้างฐานข้อมูล:

เปิดเว็บบราวเซอร์ พิมพ์ localhost/phpmyadmin

สร้างฐานข้อมูลใหม่ชื่อ smart_pond_project

สร้างตารางเก็บข้อมูล (Tables):

ไปที่แท็บ SQL แล้วใช้คำสั่งสร้างตาราง 2 ตาราง คือ sensor_log (เก็บประวัติเซนเซอร์) และ control_status (เก็บสถานะปุ่มกดบนเว็บ)

(ตาราง control_status ต้องมีการ Insert ข้อมูลตั้งต้น 1 แถว คือ id=1, โหมด=AUTO)

ติดตั้งไฟล์เว็บไซต์:

นำไฟล์ PHP ทั้งหมด (index.php, insert_data.php, get_control.php, update_control.php) ไปวางไว้ในโฟลเดอร์ C:\xampp\htdocs\smart_pond_project\

หาหมายเลข IP ของเครื่องเซิร์ฟเวอร์:

เปิด Command Prompt (cmd) พิมพ์คำสั่ง ipconfig เพื่อดูค่า IPv4 Address (เช่น 192.168.2.8) เพื่อนำไปใส่ในโค้ด ESP32

🤖 ส่วนที่ 3: การตั้งค่า Telegram Bot
เพื่อให้ระบบสามารถส่งการแจ้งเตือนเข้ามือถือได้

เปิดแอปพลิเคชัน Telegram ค้นหาบอทชื่อ @BotFather

พิมพ์คำสั่ง /newbot เพื่อสร้างบอทใหม่และตั้งชื่อบอท

เมื่อสร้างเสร็จ จะได้รับรหัส Bot Token (จดค่านี้ไว้)

ค้นหาบอทชื่อ @userinfobot หรือ @RawDataBot เพื่อหาหมายเลข Chat ID ของตัวเราเอง

นำ Bot Token และ Chat ID ไปใส่เตรียมไว้ในโค้ด Arduino

📥 ส่วนที่ 4: การอัปโหลดโค้ดลง ESP32
ติดตั้งโปรแกรมและไลบรารี:

เปิดโปรแกรม Arduino IDE

ติดตั้งไลบรารีที่จำเป็น ได้แก่: WiFiManager, DallasTemperature, และ OneWire

แก้ไขตั้งค่าในซอร์สโค้ด:

เปิดไฟล์โค้ด sketch.ino

แก้ไข IP Address ในตัวแปร serverName และ controlName ให้ตรงกับ IP ของคอมพิวเตอร์ที่รัน XAMPP อยู่

ใส่ Bot Token และ Chat ID ที่ได้จาก Telegram

ตรวจสอบรหัสความปลอดภัย (API Key) ในโค้ดให้ตรงกับในไฟล์ insert_data.php

อัปโหลด: เสียบสาย USB เข้ากับบอร์ด ESP32 เลือก Port ให้ถูกต้อง แล้วกดปุ่ม Upload

🌐 ส่วนที่ 5: การเริ่มใช้งานจริง (How to Use)
เมื่อประกอบฮาร์ดแวร์และอัปโหลดโค้ดเสร็จสิ้น ให้ทำตามขั้นตอนต่อไปนี้:

1. การตั้งค่า WiFi ครั้งแรก (Captive Portal):

เมื่อจ่ายไฟให้บอร์ด ไฟ LED สีเขียวจะดับ บอร์ดจะปล่อย WiFi ชื่อ SmartPond_Setup

นำมือถือมาเชื่อมต่อ WiFi นี้ (รหัสผ่าน 12345678)

หน้าต่างตั้งค่าจะเด้งขึ้นมา ให้เลือกเครือข่าย WiFi บ้านของคุณ กรอกรหัสผ่าน แล้วกด Save

บอร์ดจะรีสตาร์ท และไฟ LED สีเขียวจะ ติดค้าง แปลว่าเชื่อมต่อสำเร็จ พร้อมมีข้อความเด้งเข้า Telegram

2. การใช้งาน Dashboard และแผงควบคุม:

เปิดเว็บบราวเซอร์ในคอมพิวเตอร์หรือมือถือ (ที่ต่อ WiFi วงเดียวกัน) พิมพ์ URL: http://[หมายเลข IP ของคอม]/smart_pond_project/index.php

หน้าเว็บจะแสดงค่าอุณหภูมิน้ำ ความเข้มแสง และกราฟย้อนหลังแบบ Real-time (อัปเดตอัตโนมัติ)

โหมด AUTO: ระบบจะให้เซนเซอร์คุมพัดลมและไฟเอง

โหมด MANUAL: กดปุ่มสลับโหมดบนเว็บ เพื่อปลดล็อคและกดปุ่มสั่งเปิด-ปิดพัดลมและไฟด้วยตนเองได้ทันที

3. การสังเกตการทำงาน:

ทุกๆ 10 วินาที ไฟ LED สีฟ้าจะกระพริบ 1 ครั้ง (แปลว่าส่งข้อมูลเข้า Database สำเร็จ)

ถ้าน้ำร้อนเกิน 32°C ไฟ LED สีเหลืองจะติด พัดลมจะหมุน และ Telegram จะเด้งเตือนทันที!

4. การล้างข้อมูล WiFi (Factory Reset):

หากต้องการเปลี่ยนเร้าเตอร์ ให้กดปุ่ม BOOT บนบอร์ด ESP32 ค้างไว้ 3 วินาที

ระบบจะลบรหัสผ่านเดิมทิ้ง แจ้งเตือนเข้า Telegram แล้วรีสตาร์ทกลับไปปล่อย WiFi ตัวเองอีกครั้ง
